// Skill-only plugin — skills are loaded from ./skills/ via openclaw.plugin.json
// No code registration required.
const plugin = {
  id: "x-poster",
  name: "X Poster",
  register() {},
};

export default plugin;
