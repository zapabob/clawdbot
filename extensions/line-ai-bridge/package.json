{
  "name": "@openclaw/line-ai-bridge",
  "version": "2026.2.2",
  "description": "Bidirectional bridge between LINE and AI tools (Codex, GeminiCLI, Opencode) with terminal integration",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "bun run src/webhook-server.ts",
    "start": "node dist/webhook-server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.10",
    "openclaw": "workspace:*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
