# _snapshots/

Repository or runtime snapshots that are intentionally preserved for comparison.

Do not treat `_snapshots/` as an active workspace. Add a short note or manifest alongside any new snapshot so future agents know what was captured and why.
