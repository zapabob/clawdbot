# identity/

Local identity manifests live here.

- `SOUL.md` is immutable in repository layout migrations and must not be edited as part of cleanup work.
- Treat this directory as restricted, creator-owned state.

The canonical repository root keeps `identity/` as a sanctioned overlay even though it does not exist in the upstream OpenClaw root layout.
