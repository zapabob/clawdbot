# 実装ログ - 2026-03-20

## 概要

- `qwen-hakua-core` カスタムモデルを Ollama に登録した。
- RTX 3060 GPU 上で全レイヤーが動作するよう `num_gpu 99` を設定した。

---

## 手順詳細

### 1. GGUF ファイル確認

```
C:\Users\downl\Downloads\Qwen3.5-9B-Uncensored-HauhauCS-Aggressive-Q8_0.gguf
```

存在確認済み（約9.5GB、Q8_0量子化）。

### 2. 初回モデル登録

```powershell
cd C:\Users\downl\Desktop\clawdbot-main3\clawdbot-main
ollama create qwen-hakua-core -f scripts/Modelfile.hakua
```

- GGUF 取り込み完了（sha256: `99e7f220...`）
- `qwen-hakua-core:latest` として登録（9.5 GB）

### 3. GPU オフロード設定追加

`scripts/Modelfile.hakua` に `PARAMETER num_gpu 99` を追記:

```
PARAMETER num_gpu 99   # 全レイヤーを GPU に展開
PARAMETER temperature 0.8
PARAMETER top_p 0.9
...
```

モデルを再作成（GGUF レイヤーは既存を再利用、パラメータレイヤーのみ差し替え）。

### 4. 環境情報

| 項目         | 値                             |
| ------------ | ------------------------------ |
| GPU          | NVIDIA GeForce RTX 3060        |
| VRAM         | 12,288 MiB (空き: ~10,635 MiB) |
| モデルサイズ | 9.5 GB (Q8_0)                  |
| num_gpu      | 99（全レイヤー GPU 展開）      |

VRAM に全レイヤーが収まるため CPU オフロードは発生しない見込み。

---

## 変更ファイル

| ファイル                  | 変更内容                      |
| ------------------------- | ----------------------------- |
| `scripts/Modelfile.hakua` | `PARAMETER num_gpu 99` を追加 |

---

## 確認方法

```powershell
ollama run qwen-hakua-core "hello"
ollama ps
# PROCESSOR 列が "100% GPU" になっていること
```

---

# 実装ログ - 2026-03-20 (追記) — ツールコーリング対応 & モデル優先順位変更

## 概要

- `qwen-hakua-core` が Ollama API 400 エラー（"does not support tools"）を返していた問題を修正。
- Modelfile に Qwen3 ChatML ツール対応テンプレートを明示追加し、再ビルド。
- `openclaw.json` のモデル優先順位を最終確定。

---

## 問題の根因

登録済みの `qwen-hakua-core` には `TEMPLATE {{ .Prompt }}` が埋め込まれていた。
これは GGUF ファイル内に記録された**生の補完テンプレート**であり、
Ollama のツールコーリング（`.Tools` / `.ToolCalls` / `tool_response` ブロック注入）に対応していなかった。

Ollama は最新版でツールコーリングを正式サポートしているが、
**カスタム GGUF を使う場合は Modelfile 側で明示的にチャットテンプレートを指定する必要がある**。

---

## 対処手順

### 1. `scripts/Modelfile.hakua` にツール対応テンプレートを追加

SYSTEM プロンプト・パラメータは変更せず、`TEMPLATE` ブロックのみ追加:

```
TEMPLATE """{{- if or .Tools .System }}<|im_start|>system
{{- if .System }}
{{ .System }}
{{- end }}
{{- if .Tools }}

# Tools
...（Qwen3 標準ツールコーリングブロック）...
{{- end }}<|im_end|>
{{ end }}
{{- range .Messages }}<|im_start|>{{ .Role }}
{{- if eq .Role "tool" }}
<tool_response>{{ .Content }}</tool_response>
{{- else if .ToolCalls }}
<tool_call>{"name": "...", "arguments": ...}</tool_call>
{{- else }}
{{ .Content }}
{{- end }}<|im_end|>
{{ end }}<|im_start|>assistant
"""
```

### 2. モデル再ビルド

```powershell
ollama create qwen-hakua-core -f scripts/Modelfile.hakua
```

出力に `creating new layer sha256:34126892...` が確認され、テンプレートレイヤーが更新された。

### 3. `openclaw.json` モデル優先順位を確定

```json
"primary": "ollama/qwen-hakua-core",
"fallbacks": [
  "ollama/qwen3.5:9b",
  "openai-codex/gpt-5.4"
]
```

| 順位       | モデル                   | 役割                         |
| ---------- | ------------------------ | ---------------------------- |
| primary    | `ollama/qwen-hakua-core` | Hakua Core（ツール対応済み） |
| fallback 1 | `ollama/qwen3.5:9b`      | ローカル汎用                 |
| fallback 2 | `openai-codex/gpt-5.4`   | クラウドフォールバック       |

---

## 変更ファイル

| ファイル                                          | 変更内容                                                                                 |
| ------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| `scripts/Modelfile.hakua`                         | Qwen3 ChatML ツール対応 `TEMPLATE` ブロックを追加                                        |
| `.openclaw-desktop/agents/main/agent/models.json` | `qwen-hakua-core` に `"tools": false` を追加（暫定、再ビルド後は不要だが記録として残す） |
| `.openclaw-desktop/openclaw.json`                 | primary: `qwen-hakua-core`、fallbacks: `qwen3.5:9b`, `gpt-5.4` に変更                    |

---

## 確認方法

```powershell
# テンプレート確認
ollama show qwen-hakua-core --modelfile | grep -A5 TEMPLATE
# → TEMPLATE """{{- if or .Tools .System }} が表示されればOK

# ツール呼び出しテスト（OpenClaw 経由で web 検索など）
```

---

# 実装ログ - 2026-03-20 (追記3) — アップストリームマージによる消失ファイル復元

## 概要

アップストリームマージ（`eec2e2bfab`）によって消失した VRChat・LINE 関連ファイルを
`34b4a074c8`（Add VRChat relay enhancements, LINE push command, and master scripts）から完全復元した。

---

## 消失の原因

`eec2e2bfab Merge upstream latest automatically` がローカル独自実装を上書き。
`017d76e850` での部分復元では `extensions/vrchat-relay/` のみ回復しており、
以下のファイルが取りこぼされた状態のまま残っていた。

---

## 復元ファイル一覧

### VRChat 関連（コミット `ce252e761c`）

| ファイル                                        | 種別                                      |
| ----------------------------------------------- | ----------------------------------------- |
| `extensions/vrchat-relay/src/guardian-pulse.ts` | 新規復元（Guardian Pulse 監視モジュール） |
| `src/gateway/server-methods/vrchat.ts`          | 新規復元（VRChat API ルート登録）         |
| `src/metaverse/vrchat-core.ts`                  | 新規復元（メタバース中枢コア）            |
| `src/gateway/server-methods.ts`                 | 変更復元（vrchat ルートのインポート追加） |
| `src/gateway/server.impl.ts`                    | 変更復元（metaverse モジュール統合）      |

### LINE 関連（コミット `9db84643ab`）

| ファイル                              | 種別                               |
| ------------------------------------- | ---------------------------------- |
| `extensions/line/src/push-command.ts` | 新規復元（LINE push コマンド実装） |
| `extensions/line/index.ts`            | 変更復元（push-command 登録）      |

---

## 復元手順

```bash
# VRChat
git checkout 34b4a074c8 -- \
  extensions/vrchat-relay/src/guardian-pulse.ts \
  src/gateway/server-methods/vrchat.ts \
  src/metaverse/vrchat-core.ts \
  src/gateway/server-methods.ts \
  src/gateway/server.impl.ts

# LINE
git checkout 34b4a074c8 -- \
  extensions/line/src/push-command.ts \
  extensions/line/index.ts
```

---

## 教訓

アップストリームマージ時は必ず以下を確認すること：

- `src/gateway/server-methods/` 以下のカスタムルート
- `src/metaverse/` ディレクトリ
- `extensions/*/src/` のカスタムモジュール（guardian-pulse 等）
- `extensions/line/src/push-command.ts`

---

# 実装ログ - 2026-03-20 (追記2) — ツール呼び出し実動作検証

## 概要

`scripts/Modelfile.hakua` のツール対応テンプレートで再ビルドした `qwen-hakua-core` に対し、
Ollama Chat API を直接呼び出して動作を実証した。

---

## テスト内容

```bash
curl http://localhost:11434/api/chat -d '{
  "model": "qwen-hakua-core",
  "messages": [{"role": "user", "content": "hello"}],
  "tools": [{
    "type": "function",
    "function": {
      "name": "get_weather",
      "description": "Get weather",
      "parameters": {
        "type": "object",
        "properties": {"location": {"type": "string"}},
        "required": ["location"]
      }
    }
  }]
}'
```

---

## 検証結果

| 確認項目                 | 結果                                            |
| ------------------------ | ----------------------------------------------- |
| HTTP ステータス          | **200 OK**（400エラー完全解消）                 |
| ツール API 受け付け      | ✅                                              |
| `<think>` 思考モード動作 | ✅                                              |
| ツール存在の認識         | ✅（"You can ask me about the weather" と言及） |
| 初回ロード時間           | 約3分31秒（9.5GB Q8_0、RTX 3060 VRAM展開）      |
| eval_count               | 31 tokens                                       |
| total_duration           | 211,652,623,800 ns（≒ 211秒）                   |

### レスポンス抜粋

```json
{"role":"assistant","content":"<think>"}
{"role":"assistant","content":"\n\n"}
{"role":"assistant","content":"</think>"}
{"role":"assistant","content":"Hello! How can I help you today? You can ask me about the weather, or anything else you'd like to discuss."}
{"done":true,"done_reason":"stop","total_duration":211652623800,"load_duration":210091402800,"prompt_eval_count":99,"eval_count":31}
```

---

## 結論

- **400エラーは完全に解消**。`scripts/Modelfile.hakua` の Qwen3 ChatML テンプレートが正しく機能している。
- Qwen3 の thinking mode（`<think>` タグ）も正常動作。
- `qwen-hakua-core` は OpenClaw の primary モデルとしてツール呼び出し込みで本稼働可能な状態。

---

# 実装ログ - 2026-03-20 (追記4) — server.impl.ts ビルドエラー修正

## 概要

`34b4a074c8` からの復元で取り込んだ `src/gateway/server.impl.ts` に、
アップストリームコミット `455430a6f8`（"Dead code: remove unused helper modules"）で
削除済みの2ファイルへのインポートが残っており、ビルドエラーが発生していた。

削除済みファイル：

- `src/infra/env-file.ts`（`upsertSharedEnvVar` を export していた）
- `src/wizard/onboarding.ts`（`runOnboardingWizard` を export していた）

VRChat 統合コードは維持しつつ、死んだインポートのみを最小限で除去した。

---

## 変更内容

### 対象ファイル

`src/gateway/server.impl.ts`

### 変更箇所（4か所）

| #   | 種別               | 内容                                                                                                                                                    |
| --- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | import 削除        | `import { upsertSharedEnvVar } from "../infra/env-file.js"` を除去（行30）                                                                              |
| 2   | コードブロック削除 | `upsertSharedEnvVar` 使用ブロック（旧行369〜384）を除去。`OPENCLAW_GATEWAY_TOKEN` を `.env` に同期する処理（起動時のみ実行、ランタイムに実害なし）      |
| 3   | import 削除        | `import { runOnboardingWizard } from "../wizard/onboarding.js"` を除去（行58）                                                                          |
| 4   | フォールバック変更 | `opts.wizardRunner ?? runOnboardingWizard` → `opts.wizardRunner` に変更。`wizardRunner` が `undefined` の場合はオンボーディングをスキップする動作になる |

---

## ビルド確認

```bash
cd C:\Users\downl\Desktop\clawdbot-main3\clawdbot-main
node scripts/tsdown-build.mjs
# EXIT: 0（エラーなし）
```

残る警告は `src/channels/plugins/bundled.ts` の `extensions/line/index.ts` からの
`linePlugin` / `setLineRuntime` 欠落（既存問題、今回の変更とは無関係）。

`pnpm run build` 全体は `canvas:a2ui:bundle` ステップで別問題
（`vendor/a2ui/renderers/lit/tsconfig.json` 不在）により失敗しているが、
これも今回の変更スコープ外。

---

## 変更ファイル

| ファイル                       | 変更内容                                                  |
| ------------------------------ | --------------------------------------------------------- |
| `src/gateway/server.impl.ts`   | 削除済みモジュールへのインポート・使用箇所を除去（4か所） |
| `_docs/impl-log-2026-03-20.md` | 本追記                                                    |
