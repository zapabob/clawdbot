# 実装ログ - 2026-03-09

## セッション①: モデル設定 + LINEプラグイン導入

### 変更ファイル

- `.openclaw-desktop/agents/main/agent/models.json`

### 変更内容

`openai-codex` プロバイダーを追加し、`openclaw.json` のフォールバック設定が解決されない問題を修正。

```json
"openai-codex": {
  "baseUrl": "https://api.openai.com/v1",
  "auth": "oauth",
  "api": "openai-completions",
  "models": [
    { "id": "gpt-5.4",       "contextWindow": 200000, "maxTokens": 16384, "reasoning": true  },
    { "id": "gpt-5.3-codex", "contextWindow": 128000, "maxTokens": 8192,  "reasoning": false },
    { "id": "gpt-5.2",       "contextWindow": 128000, "maxTokens": 8192,  "reasoning": false }
  ],
  "apiKey": "OPENAI_API_KEY"
}
```

### 起動済み設定（要点）

| 設定                           | 値                                                 | 状態 |
| ------------------------------ | -------------------------------------------------- | ---- |
| `openclaw.json` primary        | `ollama/qwen3.5:9b`                                | OK   |
| `openclaw.json` fallbacks[0]   | `openai-codex/gpt-5.4`                             | OK   |
| `openclaw.json` timeoutSeconds | `900`（15分）                                      | OK   |
| `auth-profiles.json`           | `openai-codex:default` OAuth設定                   | OK   |
| LINEプラグイン                 | `extensions/line/` + `channels.line.enabled: true` | OK   |

---

## セッション②: デスクトップショートカット整備

### 背景

`Clawdbot-Master.lnk` が `scripts/clawdbot-master.ps1` を指していたが、コミット `afaa8d083f`（"chore: remove file."）で削除されていた。

```
Clawdbot-Master.lnk
  -> powershell.exe -ExecutionPolicy Bypass -File "scripts/clawdbot-master.ps1"  -> 404
```

### 変更ファイル

- `scripts/clawdbot-master.ps1`（再作成）

### 変更内容

旧スクリプト（マスターオーケストレーター）を v3.2 として復元。
現在のスタック（`launch-desktop-stack.ps1`）が ngrok / VOICEVOX / Gateway / TUI / Browser をカバー済みのため、
Ollama チェックのみ先行して行い、以降は `launch-desktop-stack.ps1` へ委譲する構成にした。

```
[Pre] Ollamaプロセスチェック
       -> 未起動なら ollama serve (Minimized)
[委譲] launch-desktop-stack.ps1 -SpeakOnReady
```

### 起動確認

```powershell
> powershell -File scripts/clawdbot-master.ps1

========================================
 Clawdbot Master System v3.2
 Brain  : ollama/qwen3.5:9b
 Fallbk : openai-codex/gpt-5.4
 TTS    : VOICEVOX
========================================
[Pre] Checking Ollama (qwen3.5:9b brain)...
Handing off to OpenClaw Desktop Stack...
```

### ショートカット状態（整理後）

| ショートカット               | ターゲット                                                 | 状態 |
| ---------------------------- | ---------------------------------------------------------- | ---- |
| `Clawdbot-Master.lnk`        | `scripts/clawdbot-master.ps1`                              | OK   |
| `OpenClaw Desktop Stack.lnk` | `scripts/launchers/launch-desktop-stack.ps1 -SpeakOnReady` | OK   |
| `OpenClaw-Launcher.bat`      | `OpenClaw-Launcher.ps1`（UIメニュー）                      | 保留 |
| `OpenClaw-All-In-One.bat`    | SBV2-TTS.lnk / HakuaAvatar.lnk を起動                      | 保留 |
